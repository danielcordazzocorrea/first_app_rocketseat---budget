import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 8,
        padding: 15,
        backgroundColor: '#fff',
        margin: 10,
        borderRadius: 15,
    },
    title: {
        fontSize: 20,
        color: "#000000",
    }
});