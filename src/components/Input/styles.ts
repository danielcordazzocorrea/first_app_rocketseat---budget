import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        padding: 20,
        backgroundColor: '#fff',
        borderRadius: 10,
        fontSize: 16,
        marginBottom: 20,
    }
});